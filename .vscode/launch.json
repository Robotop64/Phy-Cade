{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "(lldb) Launch",
      "type": "cppdbg",
      "request": "launch",
      "program": "${workspaceFolder}/out/bin/Test.exe",
      "cwd": "${workspaceFolder}/out"
    }
  ]
}